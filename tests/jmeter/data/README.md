# JMeter test data

Place CSV files consumed by `CSV Data Set Config` here.

- `params.csv`: sample user/password pairs used by the baseline test.

Keep files small in the repo; use larger datasets from secure storage when running full tests.
